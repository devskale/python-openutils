{
  "_doc": "Special-params index. Keys: 'provider/model' (model-specific) or 'provider' (provider-wide fallback). A model entry overrides a provider entry. Consulted by the capability probes (add an entry instead of generalising code). Example: temperature for models/providers that reject the default 0.7.",
  "moonshot/kimi-k2.7-code": { "temperature": 1.0, "note": "only temperature=1 accepted" },
  "moonshot/kimi-k2.7-code-highspeed": { "temperature": 1.0, "note": "only temperature=1 accepted" }
}
